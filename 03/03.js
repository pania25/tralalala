var q = 152;
var w = 52;
var e = 600;
if ( q < w){
    if (q < e){
        console.log ( " минимальная переменная q = " + q);
    }
}
if ( w < q){
    if (w < e){
        console.log (" минимальная переменная w = " + w);
    }
}
if ( e < q){
    if (e < w){
        console.log (" минимальная переменная e = " + e);
    }